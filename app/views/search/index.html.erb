<!DOCTYPE html>
<html>
<head>
  <title>Pawsibilities</title>
</head>
<body>

<h1 class="search-title"> Find-A-Pet </h1>

<%= render 'search/initial_form' %>

  <p>Found <b><%= @pets.count %></b> matches.</p>

  <% @pets.each do |pet| %>
  <div class="card">
    <p>
      <img src=<%= pet.photo %>/><br>
      <%= pet.name %>
    </p>
  </div>
  <% end %>


</body>
</html>
