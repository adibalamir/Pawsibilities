  <% @pets.each do |pet| %>
    <div class="card">
      <%= link_to pet do %>
        <img src=<%= pet.photo %> width="250" height="250"/><br/>
      <% end %>
      <div class="pet-name">
        <%= (pet.name).gsub("&amp;", "&") %>
      </div>
    </div>
  <% end %>