<%= render 'layouts/top_nav' %>

<div class="edit-profile">
  <%= form_with model: @shelter, local: true do |form| %>

    <% if @shelter.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@shelter.errors.count, "error") %> prohibited
          this shelter from being saved:
        </h2>
        <ul>
          <% @shelter.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
      <%= form.label :name %>
      <%= form.text_field :name, class: "form-input" %>
      <%= form.label :phone %>
      <%= form.text_area :phone, class: "form-input" %>
      <%= form.label :address %>
      <%= form.text_area :address, class: "form-input" %>
      <div class="form-submit">
      <%= form.submit %>
      </div>

  <% end %>
</div>